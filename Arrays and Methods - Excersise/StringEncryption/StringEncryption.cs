using System;
class StringEncryption
{
    static void Main()
    {
        int n = int.Parse(Console.ReadLine());
        for (int i = 0; i < n; i++)
        {
            var currentWords = Console.ReadLine().ToCharArray();
            var first = currentWords[0];
            var second = currentWords[currentWords.Length - 1];


        }
    }
}

